[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'gunicorn main:app --bind 0.0.0.0:${PORT:-8080} --workers 1 --timeout 120'"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
